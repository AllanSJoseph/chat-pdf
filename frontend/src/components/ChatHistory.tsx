import React, { useEffect } from 'react';
import api from '../api';

const ChatHistory = () => {
    useEffect(() => {
        // Fetch chat history from backend API  
    },[]);

    return (
        <div>   </div>
    );
}

export default ChatHistory;
